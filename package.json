{
  "name": "gm",
  "version": "0.1.0",
  "description": "GM - launcher de sala tipo consola para PC (Tauri + Svelte)",
  "type": "module",
  "scripts": {
    "setup": "node scripts/setup.mjs",
    "go": "node scripts/dev.mjs",
    "web": "vite --open",
    "dist": "tauri build",
    "bundle": "node scripts/bundle.mjs",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "tauri": "tauri"
  },
  "dependencies": {
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-shell": "^2"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^5",
    "@tauri-apps/cli": "^2",
    "svelte": "^5",
    "vite": "^6"
  }
}
